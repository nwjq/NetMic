## NetMic MVP 规划与边界文档（不含代码）

### 1. 目标与范围

**目标**
在局域网内，将一台设备（客户端：macOS/Linux）的本地音频输入（默认麦克风）实时传输到另一台设备（服务端：Linux），并在 Linux 上注入为**系统可选择的麦克风输入设备**（虚拟麦克风 / virtual source），供任意应用使用。

**MVP 范围**

* 单向音频流：Client → Server
* 单客户端占用：同一时刻仅允许 1 个客户端向服务端推流
* 跨平台：客户端支持 macOS/Linux；服务端仅 Linux（注入虚拟麦克风）
* GUI：配置 + 状态监控 + 日志（不做复杂音频路由图形化）
* Linux 注入方式：仅支持 **PipeWire / Pulse 兼容层**（pipewire-pulse 或 pulseaudio 环境）

**非目标（MVP 不做）**

* macOS 作为服务端并注入系统麦克风
* P2P 多节点对等/自动发现/多路流/混音
* 公网/NAT 穿透与高安全传输（可预留字段，不实现强安全）
* 更低栈兼容（纯 ALSA、JACK 原生注入等）

---

### 2. 用户流程（MVP）

**服务端（Linux）**

1. 选择 Server 模式
2. 设定监听端口（默认端口）
3. 一键创建/启用虚拟麦克风（PipeWire/Pulse 兼容层）
4. 显示：当前连接、参数、延迟/缓冲、丢包、音量电平、日志
5. 可手动断开当前客户端（释放占用）

**客户端（macOS/Linux）**

1. 选择 Client 模式
2. 输入 Server IP:Port（默认端口）
3. 选择输入设备（默认系统麦克风）
4. 配置编码与音频参数（在安全范围内）
5. 开始推流、显示状态与实时指标

---

### 3. 核心约束与边界

**单客户端策略**

* Server 只允许 1 个 active sender
* 冲突行为：当新客户端尝试连接时，Server 返回“BUSY”，客户端提示；可选“强制抢占”（MVP 可做开关，但默认关闭）

**可调参数与“安全范围”**
GUI 允许调参，但必须在安全范围内，并且有“推荐值/默认值/回退规则”。

#### 3.1 编码（2 种）

* **Opus（推荐默认）**：低延迟、抗丢包；适合语音/通用音频
* **PCM（调试/最低复杂度）**：带宽大；用于定位问题或极低延迟需求

#### 3.2 采样率（目标=48k，支持重采样）

* GUI 允许选择：**16k / 24k / 32k / 44.1k / 48k**
* **安全范围**：16k–48k
* **默认**：48k
* **回退**：若输入设备不支持所选采样率，客户端启用重采样到目标采样率

#### 3.3 声道

* MVP 固定：**mono**（简化带宽、注入与应用兼容）
* 非目标：stereo（后续版本）

#### 3.4 chunk（帧长/包时长）

* GUI 可选：**10 / 20 / 40 / 60 ms**
* **安全范围**：10–60ms
* **默认**：20ms
* 说明：帧长越短延迟越低但更敏感；越长越稳但延迟更高

#### 3.5 比特率（按编码类型）

* **Opus**（mono）

  * 安全范围：**16–128 kbps**
  * 默认：**32 kbps（语音）或 48 kbps（通用）**

* **PCM**

  * 不设“比特率”，由采样率 × 位深 × 声道决定（MVP 固定 16-bit）

#### 3.6 buffer（接收端目标缓冲 / jitter buffer）

* GUI 可调：**40–400 ms**
* 默认：**100 ms**
* 建议档位：40/60/80/100/150/200/300/400
* 规则：buffer 太小会爆音/卡顿；太大会明显口型延迟。MVP 以稳定优先。

---

### 4. 重采样与格式归一化（MVP 必须）

**目标**：无论输入设备实际输出什么格式，网络与服务端注入统一为一个“内部标准格式”，减少复杂度。

* 内部标准建议：**PCM 16-bit, mono, 48kHz**
* 客户端：采集 →（必要时重采样/混音到 mono）→ 编码（Opus/PCM）
* 服务端：解码 → 得到内部标准 PCM → 写入虚拟麦克风

**回退策略**

* 如果重采样失败/设备不可用：提示用户并停止推流
* 如果编码器不支持当前参数组合：自动回退到最接近的安全默认值，并在 UI 明确提示“已回退到 X”

---

### 5. 网络与协议边界（MVP）

**传输层选择（建议固定，避免扩散）**

* MVP：**UDP**（音频实时优先）
* 非目标：TCP（可后续提供“兼容模式”）

**控制面 vs 数据面（建议保留结构）**

* 数据面：音频帧（UDP payload）
* 控制面：少量控制报文（握手/参数协商/心跳/统计/断开）
* MVP 可复用同端口不同消息类型，但结构要明确，以便未来扩展对等模式

**最小会话能力**

* 握手：Client 发送目标参数，Server 返回接受/拒绝 + 生效参数
* 心跳：用于断线检测
* 统计：丢包率、接收缓冲深度、端到端估算延迟

**安全（MVP 边界）**

* 不做强加密/鉴权
* 预留：token/nonce 字段与“配对码 UI”入口（可隐藏在高级设置），但默认不启用

---

### 6. Linux 虚拟麦克风注入边界

**仅支持：PipeWire/Pulse 兼容层创建虚拟麦克风**

* 前置条件：系统需有 pipewire-pulse 或 pulseaudio，并可使用 pactl 创建虚拟 source/sink 类设备
* 应用可见性：虚拟麦克风在系统输入设备列表中可选
* MVP 不处理：

  * 纯 ALSA loopback
  * JACK 原生桥接
  * Wayland/桌面环境差异导致的特殊路由问题（可在 FAQ 记录）

---

### 7. GUI 需求（MVP）

**共同**

* 模式切换：Client / Server
* 配置页：IP、端口、编码参数（带安全范围与默认一键恢复）
* 状态页：连接状态、当前生效参数、实时指标
* 日志页：可导出（用于排障）

**客户端专属**

* 输入设备选择（默认系统麦克风）
* 权限状态（macOS 麦克风权限检测与引导）
* 推流按钮、自动重连开关

**服务端专属**

* 虚拟麦克风创建/移除（或启用/禁用）
* 当前客户端信息（IP、连接时长）
* 强制断开按钮
* 指标：丢包、buffer 深度、音频电平（至少 RMS/peak）

---

### 8. 状态机与错误处理（MVP）

**Server 状态**

* Idle → Listening → Connected → Streaming
* Streaming 中断：NetworkLost → Recovering（等待重连窗口）→ Listening

**Client 状态**

* Idle → Connecting → Streaming
* 断线：Reconnecting（指数退避，最大间隔上限）→ Streaming 或 Error

**关键错误分类**

* 音频设备错误（无权限/设备不可用/采样率不支持）
* 编码器错误（参数非法/初始化失败）
* 网络错误（端口占用/不可达/丢包过高导致不可用）
* 注入错误（虚拟麦克风创建失败/写入失败）

**对用户的最小可理解提示**

* 明确指出：失败环节（采集/编码/网络/注入）与建议动作（切换设备、恢复默认参数、增大 buffer）

---

### 9. 里程碑与验收标准（MVP）

**Milestone 0：参数与注入验证**

* Linux 能创建虚拟麦克风且系统可选
* 服务端可持续写入 PCM 到虚拟麦克风（本地生成测试音即可）

**Milestone 1：端到端基本通路**

* 客户端采集 → 发送 → 服务端接收 → 注入
* 默认参数（Opus/48k/mono/20ms/buffer=100ms）跑通

**Milestone 2：可调参数 + 回退**

* GUI 可调 codec/采样率/比特率/chunk/buffer（受安全范围限制）
* 生效参数回显 + 回退提示可用

**Milestone 3：稳定性与可用性**

* 连续运行 30 分钟：无周期性爆音（漂移可控）
* 断网恢复：10 秒内自动恢复（在同网内）
* 指标与日志可用于定位问题

---

### 10. 已知风险与控制策略

**风险：漂移/抖动导致音频周期性卡顿**

* 控制：目标 buffer + 漂移控制策略（维持 buffer 深度）
* UI 允许调 buffer，默认稳态 100ms


**风险：不同 Linux 环境的 PipeWire/Pulse 行为差异**

* 控制：明确最低环境假设；提供自检（检测 pactl/pipewire-pulse）与 FAQ

---

### 11. 未来扩展预留（不进入 MVP）

* macOS 服务端注入（需要虚拟音频驱动路线）
* P2P 对等：任何节点可同时收发；多会话管理
* 多客户端混音/切换
* mDNS 自动发现
* 加密与鉴权（DTLS/Noise/PSK）
* stereo 与更高阶音频路由

---

### 12. 默认值汇总（MVP 推荐默认）

* 编码：Opus
* 采样率：48k（内部标准）
* 声道：mono
* chunk：20ms
* Opus bitrate：48kbps（通用）/ 32kbps（语音）
* buffer：100ms
* 单客户端占用：启用
* UDP：启用

### 13.初步技术方案(后续可在实际开发过程中修改)


#### GUI（跨平台桌面）

* **tauri**：Rust 后端 + Web UI 的桌面框架（体积/资源占用相对轻），适合“GUI 薄、后端重”的结构。([Tauri][1])

#### 音频采集（Client 侧：macOS/Linux）

* **cpal**：跨平台音频 I/O（含输入流采集）。([Docs.rs][2])

#### 重采样（Client 侧必需）

* **rubato**：分块处理的采样率转换库（适合实时/流式按 chunk 处理）。([Crates][3])
* **resampler**：较轻量的重采样库（可作为备选对比）。([Docs.rs][4])

#### 编码/解码

##### Opus（推荐默认）

* **opus**：libopus 的 Rust 高层绑定。([Docs.rs][5])
* **audiopus**：另一套较高层的 Opus 绑定（版本较老，但可参考 API 设计）。([Crates][6])


##### PCM

* 不需要专门 codec crate（注意统一内部格式：如 S16LE/mono/48k）。

#### 网络传输（UDP + 异步运行时）

* **tokio**：常用异步运行时，包含 UDP socket。([Docs.rs][5])（opus 源里不含 tokio；若你需要 tokio 文档我可以再补一次检索）
* 可选（未来/高级）：**quinn (QUIC)**，如果你未来想要“可靠性更强 + 加密 + 低 HOL blocking”的传输层，但这会改变协议与调参模型。([Crates][9])

#### 抖动缓冲（jitter buffer）

* **neteq**：一个 libWebRTC 启发的音频 jitter buffer（适合你这种 UDP 音频接收端）。([Crates][10])
  （你也可以先自研最小 jitter buffer，但用成熟库能更快收敛“buffer 安全范围”。）

#### Linux 端注入虚拟麦克风（PipeWire/Pulse 兼容层）

* **libpulse-binding**：PulseAudio 客户端库绑定（更全 API）。([Docs.rs][11])
* **libpulse-simple-binding**：更偏“simple API”的写入（适合把解码后的 PCM 持续写到目标 sink/source）。([GitHub][12])
  （你的做法是：用 pactl 创建 Virtual Source，然后用 pulse simple API 往对应目标写 PCM。）

#### 日志与可观测性（强烈建议）

* **tracing**：异步/多线程场景更好用的日志/链路追踪。([Docs.rs][13])

